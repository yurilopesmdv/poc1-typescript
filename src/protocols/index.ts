export type GameCreated = {
    name: string,
    price: number
}

export type Game = {
    id: number,
    name: string,
    price: number
}

export type Error = {
    type: string,
    message: string
}
